# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo

# Node / Astro
node_modules/
dist/
.astro/

# Logs
*.log
npm-debug.log*

# Local env
.env
.env.local

# Local handoff snapshots (contain secrets — don't commit)
HANDOFF.md
HANDOFF-*.md

# Lighthouse audit output (generated, not committed — large + changes every run)
docs/audit/a11y-latest.json

# Claude Code local settings (may contain tokens — never commit)
.claude/settings.local.json
